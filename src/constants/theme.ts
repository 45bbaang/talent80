export const C = {
  // Backgrounds
  bg:          '#FFF5EC',   // warm cream page background
  surface:     '#FFF8ED',   // card / panel fill
  surfaceAlt:  '#FFF3D8',   // highlighted area (stepper, badge bg)

  // Brand
  primary:     '#E8B54A',   // butter yellow — main buttons, accents
  primaryDeep: '#7B5230',   // warm dark brown — dark buttons & text

  // Text
  textDark:  '#5C3D1E',     // headings, labels
  textMid:   '#9C7A5A',     // secondary text
  textLight: '#C8A880',     // placeholders, hints

  // Semantic
  earn:     '#4D8A3A',      // positive / earned (soft forest green)
  spend:    '#D46848',      // negative / spending (warm coral)

  // Structure
  border:   '#EAD9C0',      // card borders, dividers
  shadow:   '#C4A882',      // shadow tint (warm, not black)

  // Frame (outside mobile container)
  frame:    '#D4C8A8',
};

export const R = {   // border-radius presets
  sm:  8,
  md:  16,
  lg:  22,
  xl:  28,
};

export const shadow = {
  shadowColor:   C.shadow,
  shadowOpacity: 0.18,
  shadowRadius:  8,
  elevation:     2,
};
